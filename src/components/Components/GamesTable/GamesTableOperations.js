function updateOwnedStatus(ownedStatus, BGGid) {
  console.log(ownedStatus, BGGid);
  // const { dispatch } = this.props;
// if (ownedStatus) {
//   dispatch({ type: 'ADD_GAME_TO_USERS_LIBRARY', payload: BGGid });
// } else {
//   dispatch({ type: 'REMOVE_GAME_FROM_USERS_LIBRARY', payload: BGGid });
// }
}

export {
  updateOwnedStatus
};
